-- local/pastel-60s.nvim/plugin/pastel-60s.lua
-- This file is automatically sourced by Neovim on startup.

-- As recommended in the blog post, it's good practice to add a version check.
if vim.fn.has("nvim-0.7.0") ~= 1 then
  vim.api.nvim_err_writeln("Pastel-60s.nvim requires at least nvim-0.7.0.")
  return
end
